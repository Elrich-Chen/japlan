{
  "name": "virgil",
  "version": "0.0.1",
  "private": true,
  "description": "Virgil — AI group-trip companion (Linq + Cloudflare Agents + Browserbase)",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "linq:send": "node scripts/linq-send.mjs",
    "linq:webhook": "node scripts/linq-webhook-create.mjs",
    "sync:dev-vars": "node scripts/sync-dev-vars.mjs"
  },
  "dependencies": {
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250726.0",
    "typescript": "^5.8.2",
    "vitest": "^3.0.9",
    "wrangler": "4.28.0"
  },
  "engines": {
    "node": ">=20"
  }
}
